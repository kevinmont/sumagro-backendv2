import {Request, Response} from 'express';
import * as log4js from 'log4js';

const logger= log4js.getLogger();
logger.level='debug';

export default class CodesController{
    
    async vinculateCodeRegions(req:Request,res:Response){

    }
}